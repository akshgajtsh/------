#"JavaScript データ型"と調べてデータ型を3つ調べてください
・数値型
Javascriptの数値型は、typeof演算子を使ってnumberと表記されます。
Javascriptで扱われる数値型は、全て64ビットで浮動小数として表現されます。

let number1= 1;
let number2= 2;
console.log(number1 + number2);

・文字列型
Javascriptの数値型は、typeof演算子を使ってstringと表記されます。
文字が連結した列であることを意味します。
文字列を扱うために、文字コードとしてUnicode、エンコード方式としてUTF-16を採用しています

let string1="文字列";
let string2='文字列';

console log=(string1);
console log=(string2);

・論理型
ある条件下で値true（真）かfalse（偽）か判定させるために、使われているのが論理型です。
true（真）、false（偽）の値のことを「論理値」または「真偽値」と呼びます。
Javascriptの数値型は、typeof演算子を使ってtlueとfalseの値を調べるとbooleanと表記されます。

#変数に値を格納する時の用語は?
変数に値を格納することを「代入」という。

#ディレクトリとは?
パソコン用語におけるディレクトリとは、階層構造を持ったファイルを保管する為の場所
の総称の一つの呼び方。
WindowsやMacでは「フォルダ」と呼ばれる。

#絶対パスとは?
最上位にあるディレクトリから、対象のファイルまでの道順をすべて記述する方法。
https://ドメイン/フォルダ名/ファイル名
上記のように指定し、ドメインから対象までの経路を記述します。

#相対パスとは?
現在存在しているファイルから対象までのファイルまでの道順を記述する方法になります。
目的のファイルが同じ階層の存在する場合は./ファイル名
1つ上の階層に存在する場合
../フォルダ名/ファイル名
階層が上がる度に../は増えていく。